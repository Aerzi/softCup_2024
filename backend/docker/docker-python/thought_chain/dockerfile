# 使用官方的 Python 镜像作为基础镜像
FROM python:3.9-slim

# 设置工作目录
WORKDIR /thought_chain

# 复制应用程序的依赖文件到容器
COPY requirements.txt .

# 安装依赖
RUN pip install -r requirements.txt

# 复制应用程序代码到容器
COPY . .

# 暴露应用使用的端口（例如5000）
EXPOSE 8890

# 启动应用程序
CMD ["python", "websocket_server.py"]
